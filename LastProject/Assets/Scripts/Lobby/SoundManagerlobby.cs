using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundManagerlobby : MonoBehaviour
{
    // Start is called before the first frame update

    public AudioSource bgm;

    void Start()
    {
        bgm.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
